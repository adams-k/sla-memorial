version: 1
frontend:
    phases:
        prebuild:
          commands:
            - 'npm ci'
        build:
            commands:
                - 'npm run build'
    artifacts:
        baseDirectory: './sla-memorial/build'
        files:
            - '**/*'
    cache:
        paths: 'node_modules/**/*'